syntax = "proto3";

package admin;

service Admin {
  rpc Signin (SigninDto) returns (SigninResponse);
}

message SigninDto {
  string email = 1;
  string password = 2;
}

message SigninResponse {
  string access_token = 1;
}