syntax = "proto3";

package customers;

service Customers {
  rpc Signup (SignupDto) returns (SignupResponse);
}

message SignupDto {
  string first_name = 1;
  string last_name = 2;
  string email = 3;
  string password = 4;
}

message SignupResponse {
  string access_token = 1;
}
